import 'package:flutter/material.dart';

class CollapsibleNavigationDrawer extends StatelessWidget {
  const CollapsibleNavigationDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
